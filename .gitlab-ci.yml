image: node:latest

stages:
  - test

# Cache using branch name
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules
    - .next

# Tests for gitlab
test:
  stage: test
  script:
    - npm ci --include=dev
    - npm install Xvfb
    - npm run test
    - npm run lint
